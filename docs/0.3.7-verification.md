# 0.3.7 本机验收记录

环境：Windows，Grok Bot 0.57.1 / Electron 42.1.0，官方安装路径 `D:\grok_bot\Grok Bot\Grok Bot.exe`。

## 自动化检查

- Rust 常规测试：68 通过，4 项实机测试默认忽略。
- `cargo test --lib -- --ignored --test-threads=1`：4 项实机测试通过。
- `native-titlebar.test.cjs` / `early-transparency.test.cjs`：5 通过。
- 正式 `cargo build --release`：通过，未启用 `integration-test-pipe`。
- `git diff --check`：通过。

fuse 回归覆盖：精确还原、幂等恢复、官方更新拒绝覆盖、备份损坏、记录损坏、错误目标、多字节补丁拒绝、独占锁、EXE 被锁、非法 fuse、原本已启用的 fuse 保持不变、孤立备份保留、过期恢复错误不阻断新事务。

## 实机检查

- 宿主加载安装目录中的 0.3.7；真实用户 profile 仍由宿主提供，图片背景 `activeTargets=1`。
- 从原始 EXE 无参数普通启动后，watcher 自动重启接管成功，背景与原生标题栏桥正常。
- 原生标题栏桥就绪，主进程 9238 Inspector 在注入后关闭；9337 renderer CDP 仅监听回环。
- 暂停、重新应用、页面刷新后的自动恢复通过；隐藏窗口刷新同样恢复背景。
- 隐藏页面中图片 `onload` 成功但旧 `HTMLImageElement.decode()` 挂起；改用 `createImageBitmap` 后完整解码和接管通过。
- 有效 VP8 WebM 热切换通过：`VIDEO`、`readyState=4`、`paused=false`、`muted=true`；切回图片通过。
- `restore` 成功还原磁盘 EXE 并普通重启；新主进程命令行不含调试参数。
- worker 先退出、Grok 后关闭：独立助手继续运行并还原 EXE。
- 在测试特性下模拟 CreateProcess 后、Inspector 关闭前 worker 退出：助手继续运行并关闭遗留 Inspector。
- 再结束测试助手、关闭 Grok，保留补丁和 journal；下一次 worker 启动成功恢复。
- 结束验收时原始 SHA-256 恢复，Authenticode 为 `Valid`，无 pending/error 记录、无遗留 Grok/助手进程或调试端口。

原始及还原后 EXE SHA-256：

```text
7DC5612406F21427AEB093E16839DF403776778654B30F510002E2C2EC291213
```

仅开启 Inspector fuse 时 EXE SHA-256：

```text
322E9EF1F6AE75A4AA209C3EAEE2BA8C27EDFBD697C8C6ECED407D8DC6C2338D
```

本次未做断电故障注入，也不声称助手是开机恢复服务；重启且插件未运行时的限制见 README。测试媒体保存在内存，临时服务和日志已清理。
